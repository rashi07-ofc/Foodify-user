export interface FoodType {
    name: string;
    image: string;
}

export interface Outlet {
    name: string;
    logo: string;
}

export interface Restaurant {
    name: string;
    img: string;
    rating: number;
    time: string;
}